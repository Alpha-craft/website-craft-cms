<div class="border-box md:box-content place-content-center {{bgColor}} ">
  <div class="border-green-400 border-2 transition duration-300 ease-in-out hover:scale-110">
    <h1 class="text-lg md:text-xl p-2 {{bgHeader}}">{{title}}</h1>
    {% for item in post %}
        <img src="{{image.url}}" style="height: 150px; width: 300px;" alt="">      
    {% endfor %}
  </div>
</div>